name: tests

on:
  push:
    branches:
      - fix/gemini-structured-output
      - diag/gemini-status

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm test
